name: DPL pretix
description: Event handling in pretix for DPL
package: Custom

type: module
core_version_requirement: ^10

dependencies:
  - dpl_event:dpl_event
  - recurring_events:recurring_events
  - webform:webform

configure: dpl_pretix.settings

"interface translation project": dpl_pretix
"interface translation server pattern": https://git.drupalcode.org/project/%project/-/raw/%version/translations/%project.%language.po
